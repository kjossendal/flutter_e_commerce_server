{
  "routes": [
    {
      "method": "GET",
      "path": "/payment/card",
      "handler": "payment.findcards",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/payment/card",
      "handler": "payment.addcard",
      "config": {
        "policies": []
      }
    }
  ]
}
